import type { Metadata } from "next";
import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata: Metadata = {
  title: "Manifesto",
};

export default function ManifestoPage() {
  return <PlaceholderPage title="Manifesto" />;
}
