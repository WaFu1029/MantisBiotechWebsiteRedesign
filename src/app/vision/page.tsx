import type { Metadata } from "next";
import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata: Metadata = {
  title: "Vision",
};

export default function VisionPage() {
  return <PlaceholderPage title="Vision" />;
}
